name: OTAP-Pipeline

on:
  push:
    branches:
      - Development
      - main
      - origin/pipeline-test
  pull_request:
    types: [opened, synchronize, reopened]
    branches:
      - Development
      - main

# Dit geeft GitHub Actions de rechten om de rapporten te uploaden en PR's bij te werken
permissions:
  security-events: write
  contents: read
  actions: read
  pull-requests: write

jobs:
  # SonarCloud Analysis Job
  sonarcloud:
    name: Code Coverage & Quality Analysis
    runs-on: ubuntu-latest
    steps:
      - name: Code ophalen
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Schakelt shallow clones uit voor nauwkeurigere Sonar-statistieken

      # SonarCloud vereist minimaal Java 17 om de scanner zelf te draaien
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'zulu'

      - name: Cache SonarQube packages
        uses: actions/cache@v4
        with:
          path: ~/.sonar/cache
          key: ${{ runner.os }}-sonar
          restore-keys: ${{ runner.os }}-sonar

      - name: Cache Maven packages
        uses: actions/cache@v4
        with:
          path: ~/.m2
          key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-m2

      # Voert de tests uit en stuurt de coverage-rapporten rechtstreeks door naar SonarCloud
      - name: Build and analyze
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: |
          mvn -B clean verify org.sonarsource.scanner.maven:sonar-maven-plugin:sonar \
            -Dsonar.projectKey=OpenMRS13_openmrs-module-webservices.rest \
            -Dsonar.coverage.jacoco.xmlReportPaths=target/site/jacoco/jacoco.xml
        working-directory: openmrs-module-webservices.rest

      # NIEUW: Deze stap pakt het JaCoCo-rapport en hangt het aan de pipeline run
      - name: Upload Coverage Report
        uses: actions/upload-artifact@v4
        with:
          name: code-coverage-report
          path: openmrs-module-webservices.rest/target/site/jacoco/

  # Bestaande SAST Job
  sast:
    runs-on: ubuntu-latest
    steps:
      - name: Code ophalen
        uses: actions/checkout@v4

      - name: Set up JDK 8
        uses: actions/setup-java@v4
        with:
          java-version: '8'
          distribution: 'temurin'

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: java

      - name: Build with Maven
        run: mvn -B package --no-transfer-progress
        working-directory: openmrs-module-webservices.rest

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v3

  # Bestaande Dependency Review Job
  dependency_review:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - name: Dependency Review
        uses: actions/dependency-review-action@v4
        with:
          fail-on-severity: high

  # Bestaande SBOM Job
  sbom:
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    steps:
      - name: Code ophalen
        uses: actions/checkout@v4

      - name: Set up JDK 8
        uses: actions/setup-java@v4
        with:
          java-version: '8'
          distribution: 'temurin'

      - name: Build project
        run: mvn -B package --no-transfer-progress
        working-directory: openmrs-module-webservices.rest

      - name: Generate SBOM
        uses: anchore/sbom-action@v0
        with:
          format: cyclonedx-json
          output-file: sbom.cyclonedx.json
          path: openmrs-module-webservices.rest

      - name: Upload SBOM as artifact
        uses: actions/upload-artifact@v4
        with:
          name: sbom
          path: sbom.cyclonedx.json

  # Deploy naar Testomgeving Job
  deploy_test:
    if: github.event_name == 'push' && (github.ref == 'refs/heads/Development' || github.ref == 'refs/heads/origin/pipeline-test')
    runs-on: ubuntu-latest
    environment: Testomgeving
    steps:
      - name: Code ophalen
        uses: actions/checkout@v4

      - name: Set up JDK 8
        uses: actions/setup-java@v4
        with:
          java-version: '8'
          distribution: 'temurin'

      - name: Build project
        run: mvn clean package
        working-directory: openmrs-module-webservices.rest

      - name: Deploy naar Test server
        run: |
          echo "Deployen naar Test..."

  # Deploy naar Productieomgeving Job
  deploy_prod:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    environment: Productieomgeving
    steps:
      - name: Code ophalen
        uses: actions/checkout@v4

      - name: Set up JDK 8
        uses: actions/setup-java@v4
        with:
          java-version: '8'
          distribution: 'temurin'

      - name: Build project
        run: mvn clean package
        working-directory: openmrs-module-webservices.rest

      - name: Deploy naar Productie server
        run: |
          echo "Deployen naar Productie..."